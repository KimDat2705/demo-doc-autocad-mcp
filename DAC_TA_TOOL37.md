# ĐẶC TẢ TOOL #37 `doc_bang_ke_khung` — bản CHỐT sau red-team (2026-08-06)

> Nguồn: wf_689d5254 (đặc tả + prototype + 3 góc red-team + chốt). Prototype: `D:\Dat-Antigravity\_lat4\proto_khung\`.
> Nền tảng số: thước đọc tay 391 hàng (`D:\Dat-Antigravity\_lat4\su_that_nen_doc_tay.json`) + phân xử 3-bộ-trích (wf_4ff11f74).
> ⛔ CHUẨN USER: số SAI = lỗi · số THIẾU = lỗi · đọc đúng hoặc TỪ CHỐI RÕ. ⛔ CHƯA CODE — chờ user quyết 4 mục cuối file.

## Quyết định kiến trúc (Q1)

CHỐT: đường C (hybrid) tách 2 bước, cả hai vế đều có số. BƯỚC 1 (lát này): THÊM TOOL MỚI #37 `doc_bang_ke_khung` additive, #36 giữ nguyên từng byte (tiền lệ #34/#35/#36; 35 ca test #36 không đổi; rủi ro hồi quy 0). BƯỚC 2 (lát riêng, red-team 2 tầng riêng): van nhường trong #36 — hàng #36 sắp trả có handle NHÃN trùng với hàng thuộc bảng-khung của #37 thì thay bằng câu chuyển hướng không số. Cơ sở đo (do_q1.py): #36 HEAD trên 4 file kích hoạt trả 16 hàng, 10/16 hàng min/max SAI so với sự-thật-nền (vd A1 'Khoảng cách giữa các mặt cắt' min36=7.0 vs thật 2.0; A2 'đáy kênh hoàn trả' 1.84 vs thật 1.74), và 16/16 hàng nằm TRONG bảng-khung #37 ⇒ van nhường xoá đúng 100% đầu ra sai đã biết, mất 0 hàng ngoài khung. KHÔNG sửa ruột #36: cấu trúc trả về khác hẳn (2 dãy, hang_tu_choi, o_chu, phân tầng) = đổi hợp đồng dữ liệu; và corpus cho thấy lớp không-khung #36 vẫn cần chạy nguyên (0 file nào #36 đọc mà #37 im → không mất gì, nhưng van chỉ được chặn hàng TRÙNG handle). Lý do không để nguyên vĩnh viễn: 10/16 hàng sai phát ra hằng ngày vi phạm chuẩn 'số SAI = lỗi'.

## Phân tầng độ tin (Q2)

Mức y đếm theo MỎ NEO THẬT (align_point khi TEXT có halign/valign≠0, ngược lại insert — đúng bài học lần-10). Cụm mức: tách khi khe > max(0.05, 0.10·h_mode); 0.10 là SỐ ĐO trên 391 hàng nền (do_tol.py): hàng phẳng max khe/h=0.0406, hàng 2-dãy=0.2036, đa mức≥0.6683 → 0.10 giữa vùng trống, biên ≥2x hai phía; sweep 0.06-0.19 cho phân tầng Y HỆT (plateau, không knife-edge). (a) PHẲNG = 1 mức + mọi ô số đồng nhất layer/height-mode → ĐỌC; đủ bằng chứng 'đúng gần tuyệt đối' TRONG hệ vẽ đã đo (2.556/2.556 ô khớp handle nền 3-thước+đọc tay; min/max 226/226) nhưng KHÔNG claim ngoài hệ. (b) HAI DÃY = đúng 2 dãy (rot-bucket %360, mức-y) + đẳng thức Decimal NGUYÊN không dung sai cd[i+1]−cd[i]==le[i], len(cd)==len(le)+1, cấm dãy lẻ toàn-0, kèm chứng cứ xen kẽ x (74/74) → ĐỌC + da_chung_minh=true (BOOL). (c) LỘN XỘN → TỪ CHỐI ĐÍCH DANH nhãn+handle+ly_do (chuỗi cố định 0 chữ số) đặt trong bang._vitri.hang_tu_choi (nhãn từ-chối không được làm bằng chứng — đo được +3 mốc mm nếu để ngoài). (+) CHÉO: 3 hàng phẳng cùng bảng, cùng n≥3 cột, C[i]==A[i]−B[i] nguyên mọi cột, cấm C toàn-0 → da_chung_minh_cheo cho cả ba (9/23 bảng F5, 69/97 cột; 14 bảng n=2 cố ý không claim). (d) 3 câu từ chối cấp file RIÊNG: loi_doc_file (9/9 file DXFStructureError thật nhận đúng, không nói 'không có bảng') · khong_khung · khung_khong_bang. Hàng chữ <2 ô số: có ô → hang_tu_choi lý do hang_chu_it_o_so; 0 ô → chỉ đếm trong _vitri. 142/162 hàng 'không chắc chắn' của nền rơi đúng tầng c; 20 hàng còn lại đọc được vì ô lạ KHÔNG phải ô số — lộ qua o_chu + co_chu_xen_khac_muc (ca A1 'Dốc dọc thiết kế': dãy số là CHIỀU DÀI đoạn dốc, trị số dốc nằm ở ô chữ '0.0%%%' — trả cả hai, dạy model không gán nhầm).

## Ngân sách + rổ neo (Q3)

gioi_han mặc định 60 (đồng bộ _BTD_CAP_TONG), trần cứng 200, đếm trên số Ô GIÁ TRỊ trả trong gia_tri/day_*; min/max/dau_tien/cuoi_cung LUÔN tính trên TRỌN hàng (hợp pháp vì hàng trọn theo khung; K4 đo bất biến với gh∈{1,6,12,60,200} — không tái phạm lỗi R3 của #36). Thứ tự ưu tiên khi vượt: bảng trên→dưới rồi trái→phải, trong bảng theo dải, trong hàng theo x tăng; hàng bị cắt → khong_day_du hàng + cờ kết quả + câu cảnh báo 0-chữ-số khuyên gọi lại nhan_chua (đo luồng thật: lọc 'cộng dồn'+gh=200 trả trọn 23 hàng b/171 ô, khong_day_du=False). o_chu trần riêng 20/hàng không tính ngân sách. RỔ NEO ĐO THẬT (mcp_bridge thật, mốc 100..20000 bước 100): Σ 5 file #36 HEAD = 67 neo/52 mốc; #37 gh60 = 160 neo/55 mốc (+3 mốc, +5,8%; A1 còn GIẢM 16→11); UNION hai tool cùng sống = 188/86 (+65% → thêm lý do cho van nhường Q1 bước 2); gh200 ≈ 117 mốc; FULL = 644 neo/180 mốc (×3,5) ⇒ KHÔNG nâng mặc định; nâng trần >200 và/hoặc đọc FULL là QUYẾT ĐỊNH CỦA USER (đánh đổi recall-danh-sách ↔ bề mặt ×1000 của _is_grounded — lỗ ×1000 nằm ở mcp_bridge, ngoài lát). Đóng kín N2 (K3, 5/5 file): rổ sau _strip_neo ⊆ số suy từ nguyên văn ô/nhãn/o_chu ĐÃ TRẢ — 0 số đếm/toạ độ/echo/nhãn-từ-chối lọt.

## Tách dãy (rotation, align_point) (Q4)

Khoá dãy = (rot bucket 10°, %360; mức-y theo mỏ neo). Mỏ neo: TEXT có (halign|valign)≠0 VÀ align_point tồn tại → align_point; halign=0 KHÔNG có align_point trong DXF → insert chính LÀ mỏ neo đúng của nó (không phải fallback); dùng insert thay align từng biến 23/23 thành 9/24 — đã tái xác nhận: đo F5 ô cộng dồn ha=4/va=0 ap.y lệch dãy lẻ đúng 0.2545 (khe/h 0.2036). MTEXT: không có halign/align_point → mỏ neo = insert; đo K7: 0 ô số MTEXT trên cả 5 file → rủi ro thấp, ghi giả định. Đẳng thức: parse Decimal từ chuỗi nguyên văn (',' → '.', '−' → '-'), so == tuyệt đối — KHÔNG assert 'đúng N số lẻ' (nguyên-cent là hệ quả của chuỗi 2 số lẻ, hàng 3 số lẻ vẫn chứng minh được); assert duy nhất = chuỗi số thuần ^[+\-−]?\d+([.,]\d+)?$; cấm dãy lẻ toàn-0 (đẳng thức rỗng nghĩa). Chỉ đúng-2-dãy mới thử; ≥3 dãy → tầng c (8/8 hàng 'Góc quay mặt bằng' F5 vào c đúng). Toàn corpus 142 file: đẳng thức chỉ nổ trên F5 (23 hàng) — 0 dương tính giả.

## Quan hệ detector cũ (Q5)

Lớp CÓ KHUNG: khung nét LÀM LUÔN việc phát hiện — không G4/G7/G10 (bộ gate đó giết 24/20/14 block F5 = lý do #36 sót trọn 23 bảng; khung vớt lại đủ). Lớp KHÔNG KHUNG: #36 chạy y nguyên — corpus đo #36 kích hoạt 0/86 file khảo_sát và 4/56 f1_check (cả 4 đều có khung, #37 phủ), tức HIỆN không thấy ca #36-ngoài-khung nào bị van nhường đụng. KHÔNG thêm từ-vựng-nhãn chặn bảng toạ-độ-mốc/bảng-thép: tool tuyên bố trung tính 'bảng kẻ khung' (không claim trắc dọc), đọc bảng thép/bê tông là ĐỌC ĐÚNG Ô và là năng lực CÓ ÍCH cho bóc khối lượng (corpus: Ket Sat 3T12P đọc 290 hàng thống kê thép nguyên văn); keyword ở tầng trích đã bị bác bởi tiền lệ garble TCVN3 — nếu cần chặn theo nghĩa thì đó là tầng routing/prompt.

## C1/C2 trắc ngang (Q6)

CHỐT: đọc-có-phân-tầng, KHÔNG từ chối cả file, KHÔNG tầng riêng. Số: C1 53 hàng phẳng đọc + 60 từ chối; C2 75 đọc + 74 từ chối; phần đọc đúng 100% theo nền (0 sai min/max trong 226 hàng a toàn cục); 142/162 hàng 'không chắc chắn' của chính nền rơi trọn tầng c — con số '37% hàng đổi min/max vì ký hiệu taluy đè' thuộc tập hàng đa-mức/lệch-thuộc-tính = nằm trong tầng từ chối. Từ chối cả file là thừa vì phần phẳng đã được nền đọc-tay + 3 bộ trích độc lập xác nhận sạch; từ chối chọn lọc đích danh mới đúng chuẩn 'đọc đúng hoặc từ chối rõ'. Tên tool không chứa 'trắc dọc' nên 'CN = cắt ngang' không còn là đọc-lạc-loại; nghĩa hàng do nhãn nguyên văn quyết.

## Cấu trúc trả về

{co_bang: bool, bang: [{hang: [{nhan (nguyên văn), handle, gia_tri: [{so, handle}], nho_nhat/lon_nhat/dau_tien/cuoi_cung: {so, handle}; tầng b thay gia_tri bằng day_cong_don/day_le: [{so, handle}] + cong_don_cuoi_cung + nho_nhat_cong_don/lon_nhat_cong_don/nho_nhat_le/lon_nhat_le; o_chu: [{chu, handle}] ≤20; co_chu_xen_khac_muc, da_chung_minh, da_chung_minh_cheo, khong_day_du: bool}], _vitri: {x_trai, x_phai, y_duoi, y_tren, buoc_hang, hang_tu_choi: [{nhan, handle, ly_do}], so_hang_doc, so_hang_tu_choi, so_hang_nhan_trong}}], khong_day_du: bool, _vitri: {so_bang, tong_gia_tri, tham_so: {nhan_chua, gioi_han}}, ghi_chu}. Kỷ luật: mọi số đếm/toạ độ/echo-tham-số + nhãn-hàng-từ-chối nằm trong _vitri (bị _strip_vitri loại khỏi rổ, model vẫn thấy); khoá handle đúng dạng 'handle' để _strip_handle nhận; số hợp lệ (ô, nhãn hàng ĐỌC, o_chu) nằm ngoài _vitri để không giết câu đúng (chống lớp lỗi A3). Kiểm K1-K5 PASS 5/5 file.

## Prose các nhánh (0 chữ số)

Tất cả 0 chữ số + không cụm _REFUSAL_MARKERS (kiểm K1/K2 tự động). (1) GHI_CHU_DOC: 'Mỗi hàng = MỘT NHÃN trong bảng kẻ KHUNG NÉT + các giá trị nằm TRONG KHUNG của hàng đó... biên hàng là nét kẻ người vẽ vẽ ra, không phải cửa sổ đoán... da_chung_minh nghĩa là hai dãy số khớp đẳng thức cộng dồn... KHÔNG tự cộng/trung bình... hàng trong hang_tu_choi là hàng MÁY TỪ CHỐI ĐỌC — đừng suy đoán giá trị... o_chu là CHỮ cùng dải kẻ... khi co_chu_xen_khac_muc bật, nghĩa đầy đủ phải đọc CẢ SỐ LẪN CHỮ — hàng độ dốc thường ghi trị số dốc bằng chữ còn dãy số là CHIỀU DÀI ĐOẠN DỐC, đừng gán dãy số làm trị số của nhãn.' (2) GHI_CHU_CAT (nối khi cắt): '...nho_nhat/lon_nhat/dau_tien/cuoi_cung vẫn tính trên TRỌN hàng nên dùng được; TUYỆT ĐỐI không kết luận danh sách đã đủ; gọi lại với nhan_chua...'. (3) TU_CHOI loi_doc_file: 'KHÔNG ĐỌC ĐƯỢC FILE — file lỗi cấu trúc DXF... Đây là LỖI ĐỌC FILE, KHÔNG phải bản vẽ thiếu bảng... xuất lại file DXF từ bản gốc.'. (4) khong_khung: 'Bản vẽ này KHÔNG có bảng kẻ khung nét đọc được... bảng dạng khác có thể vẫn tồn tại — thử doc_bang_trac_doc hoặc tim_kiem.'. (5) khung_khong_bang: 'CÓ nét kẻ khung nhưng máy không nhận ra hàng dữ liệu nào bên trong khung... Đừng kết luận bản vẽ thiếu số liệu...'. (6) ly_do từ chối hàng: 'nhieu_lop_chu_chong_nhau_trong_dai' | 'hang_chu_it_o_so'. Nguyên văn đầy đủ trong khung_doc.py.

## Xử lý từng phát hiện red-team

GÓC 1: G1-1 (CAO, kênh ×1000 F5) = NHẬN là rủi-ro-tồn-dư + việc user quyết; không vá trong tool (ô mét là dữ liệu thật, bỏ ×1000/per-claim đã NO_GO có số); lát riêng 'grounding có-đơn-vị' ở mcp_bridge xếp NGAY SAU. G1-2 (CAO, rổ giàu mở bịa trên A1/C1/C2, ca 1,94 sai được cross-ground) = NHẬN, cùng gốc G1-1, ghi vào đặc tả + đưa ca '1,94m trên C1' vào bộ câu bịa của vòng red-team sau-code. G1-3 (TRUNG, thiếu lưới mét) = NHẬN, sửa số đặc tả (khai cả 2 lưới + corpus-wide). G1-4 (TRUNG, tích luỹ phiên ~2×) = NHẬN, sửa số đặc tả; không đổi code (bản chất recall). G1-5/G1-6 (THẤP) = ghi nhận (kỷ luật đứng vững; lỗ số-ĐẾM là của HEAD, lát riêng đã có trong memory). GÓC 2: P1 (CAO) = NHẬN → vá V-A: gate da_chung_minh (so_pt≥2 AND xen_ke AND không cắt AND không tràn mép), lộ so_phuong_trinh+xen_ke vào bang._vitri.chung_minh; F5 sau gate 9/23 claim (đã tự đo). P2 (CAO) = NHẬN → vá V-B: hàng b trả o_chu+co_chu_xen_khac_muc; band 2-mức có khe ≥ ngưỡng×buoc_hang hoặc ≥2 text không-số vùng nhãn → từ chối ly_do 'nghi_tron_hang_mat_vach' (ngưỡng đo plateau lúc code, gate 0/23 hàng b F5 giết oan + S2 bị bắt). P3 (CAO) = NHẬN 3 phần: garble-normalizer 2 phía cho nhan_chua = ĐIỀU KIỆN GO (tự đo: 'kªnh' vớt 1.740, chuỗi chuẩn trượt); mục lục nhãn hàng-bị-cắt vào _vitri (V-J); ưu tiên ngân sách theo bảng-nhiều-hàng = ĐỂ USER QUYẾT (đề xuất giữ trên-xuống + mục lục). P4 (CAO) = NHẬN → V-C fail-closed đếm mọi nhánh bỏ + dải ≥2 ô số không nhãn → hang_tu_choi 'khong_thay_nhan_hang'. P5 (TRUNG) = NHẬN → V-E: nhánh 'loc_khong_khop' riêng + tách FileNotFoundError/OSError ('loi_mo_file') và Exception khác ('loi_may_doc') khỏi DXFStructureError. P6 (TRUNG, latent) = NHẬN → V-F: _anchor(ax,ay) nhất quán cả 5 chỗ + guard ap rác; gate 5-file nền bất biến. P7 (TRUNG) = NHẬN → V-G: quét đệm ngoài mép, bật khong_day_du+tran_mep_khung, hạ claim. P8 (THẤP) = NHẬN (i) hàng b bị cắt → da_chung_minh=False (gộp vào V-A; min/max vẫn trả); (ii) nhãn-hàng-trống-chứa-số → _vitri.nhan_hang_trong_co_so (V-K); (iii) hàng thieu 1 ô số = HOÃN (lát đọc-hàng-chữ); (iv) ép kiểu gioi_han = giữ (đã echo); (v) 2 giá trị chặn oan = rủi-ro-tồn-dư có chủ đích. GÓC 3: V1 (CAO) = NHẬN (gộp V-C, tiêu chí: 0 dải rơi không vết toàn corpus). V2 (CAO) = NHẬN → V-D: cụm vạch-y toàn cục dung sai RD (single-link) thay round(y,4) bằng-tuyệt-đối; gate 01-TD/2.Tdoc thoát im lặng + 5-file nền bất biến. V3 (TRUNG) = NHẬN → V-H: bảng 1-hàng AND (cao bảng >> buoc_hang hoặc giá trị lặp hệt) → hang_tu_choi 'khung_nghi_luoi_truc'; mức độ (từ chối vs chỉ cờ) ĐỂ USER QUYẾT; gate 15/15 bảng thật C1/C2 sống + 0 hàng nền hạ oan. V4 (TRUNG) = NHẬN → V-I: dedup hàng theo handle nhãn, giữ bảng hẹp nhất. V5 (TRUNG) = NHẬN, sửa số đặc tả + vòng red-team guard sau-code phải chạy trên ≥5 file kích-hoạt-mới. V6 (TRUNG) = NHẬN (trùng P5-i). V7 (TRUNG, van đổ test) = HOÃN đúng chỗ: thuộc lát van bước 2; ngay từ bây giờ khai công khai danh sách 6 ca được-phép-đổi-định-nghĩa (xem xu_ly_n2_r2). V8 (THẤP) = NHẬN (gộp V-F). V9 (THẤP) = NHẬN (gộp V-E). V10 (THẤP) = NHẬN: ghi số RAM vào đặc tả + ca test quá-cỡ MAX_BAN_VE (G-15).

## ĐẶC TẢ CUỐI (kèm 11 bản vá V-A..V-K)

TOOL #37 `doc_bang_ke_khung` — BƯỚC 1 ADDITIVE (van nhường #36 là lát riêng). PHẠM VI FILE: sửa `D:\Dat-Antigravity\HeThongThiCongXayDung-AnhTu\demo_mcp_autocad\tools_core.py` (thêm tool #37 — port từ proto `D:\Dat-Antigravity\_lat4\proto_khung\khung_doc.py` SAU KHI vá V-A..V-K; hàm #36 giữ nguyên TỪNG BYTE, diff hàm #36 = 0), thêm `tests/test_bang_ke_khung.py`; KHÔNG chạm `mcp_bridge.py` (×1000 lát riêng), KHÔNG chạm SYSTEM_PROMPT (byte-lock 239e8b7b — nếu đăng ký tool bắt buộc sửa prompt thì DỪNG hỏi user), KHÔNG chạm `tests/test_bang_trac_doc.py`. Tích hợp: #37 là method của Drawing, đứng SAU cổng MAX_BAN_VE, file quá cỡ trả NGUYÊN VĂN câu từ chối quá-cỡ #36 đang dùng; `nhan_chua` so khớp sau khi CHUẨN HOÁ GARBLE 2 PHÍA bằng bộ VNI/TCVN3 đã LIVE của sản phẩm (điều kiện GO — tự đo: chuỗi chuẩn 'đáy kênh' trượt garble, 'kªnh' mới trúng). ══ 11 BẢN VÁ PROTO (trước tích hợp) ══ V-A: da_chung_minh=True CHỈ khi so_pt≥2 AND xen_ke=True AND hàng không bị cắt ngân sách AND không tran_mep_khung; lộ per-hàng {nhan_handle, so_phuong_trinh, xen_ke} vào bang._vitri.chung_minh; hàng b trượt gate vẫn trả đủ cấu trúc 2 dãy, chỉ mất claim (F5: 9/23 giữ claim, 129/171 ô — đã đo). V-B: hàng b trả o_chu(≤20)+co_chu_xen_khac_muc như hàng a; ứng viên hàng b có ≥2 text không-số ở vùng trái ô-số-đầu (theo anchor-x) HOẶC khe 2 mức ≥ NGƯỠNG_TRỘN×buoc_hang → từ chối ly_do 'nghi_tron_hang_mat_vach' (NGƯỠNG_TRỘN đo plateau lúc code; tham chiếu: khe hàng b thật = 0.2036×h chữ << buoc_hang; gate 0/23 F5 oan + fixture S2 bị bắt). V-C FAIL-CLOSED: mọi dải bị bỏ phải để vết — dải có ≥2 ô số mà text trái-nhất là số → hang_tu_choi ly_do 'khong_thay_nhan_hang' (nhan = nguyên văn text trái nhất, nằm trong _vitri nên chữ số bị strip); dải bị CAO_MAX → đếm root._vitri.so_dai_bo_qua.qua_cao; dải win rỗng → .trong; text số trong bbox bảng không thuộc hàng nào → bang._vitri.so_o_ngoai_hang; CẤM continue câm. V-D: bỏ khoá round(lo,4)/round(hi,4) bằng-tuyệt-đối; cụm vạch-y TOÀN CỤC dung sai RD=0.05 (single-link, như cụm x), khoá dải theo đại-diện-cụm (root-cause 01-TD lệch 0.002 — đã tự xác nhận). V-E TỪ CHỐI ĐÚNG LOẠI: thêm trạng thái 'loc_khong_khop' (file CÓ hàng đọc được nhưng lọc không khớp); tách except: DXFStructureError→'loi_doc_file', FileNotFoundError/OSError→'loi_mo_file', Exception khác→'loi_may_doc'; cấp Drawing: 'qua_co'. V-F MỎ NEO NHẤT QUÁN: hàm _anchor(t)→(ax,ay) hoặc (x,y) dùng ở CẢ 5 chỗ (membership dải theo y, cửa sổ x, sort trong hàng, chọn nhãn trái nhất, kiểm xen kẽ); guard ap rác: (ha|va)≠0, ap≈(0,0,0), |ap−insert|>K×height → dùng insert (K đo lúc code). V-G TRÀN MÉP: text số cùng-mức-y trong đệm [xmin−đệm,xmin)∪(xmax,xmax+đệm], đệm = 1 bước cột trung vị của hàng (fallback 5×RD) → khong_day_du=True + tran_mep_khung=True + da_chung_minh=False; GHI_CHU nêu (0 chữ số) min/max chỉ tính trên phần trong khung khi cờ bật. V-H LƯỚI TRỤC: bảng đúng 1 hàng AND (cao bảng > NGƯỠNG_CAO×buoc_hang HOẶC mọi ô số trị lặp hệt) → hạ hàng xuống hang_tu_choi ly_do 'khung_nghi_luoi_truc' (NGƯỠNG_CAO đo plateau; gate: 15/15 bảng thật C1/C2 sống, ≥25/29 hàng pairing chế tạo hạ, 0/391 hàng nền hạ oan). V-I DEDUP KHUNG LỒNG: hàng trùng handle nhãn giữa các bảng lồng nhau → giữ ở bảng hẹp nhất (corpus: 5 hàng/10 ô). V-J MỤC LỤC CẮT: hàng a/b bị drop trọn vì ngân sách → bang._vitri.muc_luc_hang_chua_tra [{nhan,handle}]; GHI_CHU_CAT thêm câu chỉ đường (0 chữ số). V-K: hàng 'trong' có nhãn chứa chữ số → bang._vitri.nhan_hang_trong_co_so [{nhan,handle}]. ══ CẤU TRÚC TRẢ VỀ ══ {co_bang, bang:[{hang:[HÀNG_A|HÀNG_B], _vitri:{x_trai,x_phai,y_duoi,y_tren,buoc_hang, hang_tu_choi:[{nhan,handle,ly_do}], so_hang_doc,so_hang_tu_choi,so_hang_nhan_trong, chung_minh:[{nhan_handle,so_phuong_trinh,xen_ke}], muc_luc_hang_chua_tra:[{nhan,handle}], nhan_hang_trong_co_so:[{nhan,handle}], so_o_ngoai_hang}}], khong_day_du, _vitri:{so_bang,tong_gia_tri,trang_thai,so_khung, so_dai_bo_qua:{qua_cao,khong_nhan,trong,trung_vach}, tham_so:{nhan_chua,gioi_han}}, ghi_chu}. HÀNG_A = {nhan,handle,gia_tri:[{so,handle}],nho_nhat,lon_nhat,dau_tien,cuoi_cung,o_chu:[{chu,handle}]≤20,co_chu_xen_khac_muc,da_chung_minh:false,da_chung_minh_cheo,khong_day_du,tran_mep_khung}. HÀNG_B = như spec gốc + o_chu + co_chu_xen_khac_muc + tran_mep_khung, da_chung_minh theo gate V-A. KỶ LUẬT RỔ NEO (giữ nguyên tiền lệ, mở rộng cho trường mới): MỌI số đếm/toạ độ/echo/nhãn-từ-chối/mục-lục/so_phuong_trinh nằm trong _vitri (bị _strip_vitri loại, model vẫn thấy); khoá handle dạng handle_*/*_handle/handle; bool (da_chung_minh, tran_mep_khung, xen_ke, co_chu_xen_khac_muc) được ở ngoài _vitri theo tiền lệ khong_day_du; số hợp lệ (ô, nhãn hàng ĐỌC, o_chu) ở ngoài _vitri để không giết câu đúng (chống lớp A3). NGÂN SÁCH: mặc định 60, trần cứng 200 (không đổi — nâng là việc user); min/max/dau/cuoi tính trên trọn-phần-trong-khung, bất biến theo gh; O_CHU_CAP=20 ngoài ngân sách. ══ PROSE (tất cả 0 CHỮ SỐ, không cụm _REFUSAL_MARKERS, gate K1/K2 tự động; wording chốt lúc code, đối chiếu danh sách marker thật) ══ (1) GHI_CHU_DOC: giữ bản proto + thêm: 'khi tran_mep_khung bật, hàng còn phần tràn ra ngoài mép khung máy chưa đọc — nhỏ nhất/lớn nhất chỉ tính trên phần trong khung, đừng kết luận trọn hàng'; 'danh sách nhãn các hàng máy chưa trả vì chạm giới hạn nằm trong phần vị trí — hàng đó TỒN TẠI, gọi lại với nhan_chua để đọc'. (2) GHI_CHU_CAT: giữ + câu chỉ tới mục lục. (3) loi_doc_file / (4) khong_khung / (5) khung_khong_bang: giữ nguyên văn proto. (6) loc_khong_khop: bảng CÓ hàng dữ liệu đọc được nhưng không hàng nào khớp chuỗi lọc — đừng kết luận bản vẽ thiếu hàng; gọi lại không lọc hoặc đổi chuỗi lọc. (7) loi_mo_file: không mở được file ở đường dẫn đã cho (không tồn tại/không có quyền) — lỗi mở file, không nói gì về nội dung bản vẽ. (8) loi_may_doc: máy đọc gặp lỗi bất ngờ chưa rõ nguyên nhân — không kết luận về cấu trúc file hay nội dung; thử lại hoặc báo kỹ thuật. (9) qua_co: dùng NGUYÊN VĂN câu #36. LY_DO enum (6, cố định 0 chữ số): nhieu_lop_chu_chong_nhau_trong_dai · hang_chu_it_o_so · khong_thay_nhan_hang · nghi_tron_hang_mat_vach · khung_nghi_luoi_truc · (giữ chỗ) nghi_nhieu_nhan_trong_dai. TRẠNG THÁI enum: co_bang · khong_khung · khung_khong_bang · loi_doc_file · loi_mo_file · loi_may_doc · loc_khong_khop (+qua_co tầng Drawing). ĐÍNH CHÍNH SỐ TRONG DAC_TA_TOOL37.md: khai cả lưới mét (+21% default) + corpus-wide (union 133 vs 52 mốc, 26 file mới, 1-53 mốc/file) + tích luỹ phiên ~2× + RAM 212MB→1.682MB/72,4s + flagship A2 1.740/1.840 CHỈ qua nhan_chua + tự-chứng-minh sau gate = 336/462 ô (72,7%).

## Thứ tự làm B0-B7

B0 ĐÓNG BĂNG TRỌNG TÀI: xoá __pycache__; chạy lại do_5file.py + do_kyluat.py + do_corpus.py trên proto HEAD, diff với corpus_kq.jsonl/su_that_nen_doc_tay.json phải = 0 (nếu ≠0 thì DỪNG — bộ trích hỏng, tiền lệ 10 lần). B1 VÁ PROTO tại _lat4/proto_khung theo cụm, MỖI cụm một lần đo đủ (5-file nền + K1-K7 + corpus 142 diff-so-BEFORE, mọi thay đổi trạng thái/tier phải nằm TRONG danh sách kỳ vọng của cụm, ngoài danh sách = DỪNG): cụm 1 = V-C+V-D+V-E (fail-closed + quantize + từ-chối-đúng-loại; kỳ vọng đổi: 01-TD, 2.Tdoc, các file có dải im); cụm 2 = V-F+V-G (anchor + tràn mép; kỳ vọng: 5-file nền BẤT BIẾN, fixture S5/S16/S17 đổi); cụm 3 = V-A+V-B (gate chứng minh + o_chu hàng b; kỳ vọng: F5 23→9 claim, 0 hàng mất); cụm 4 = V-H+V-I+V-J+V-K (lưới trục + dedup + mục lục; kỳ vọng: ≤29 hàng hạ tầng, 5 hàng dedup, 15/15 bảng thật C1/C2 sống). B2 ĐO NGƯỠNG MỚI có plateau (NGƯỠNG_TRỘN, NGƯỠNG_CAO, đệm tràn mép, K guard ap rác) — sweep như LV_TOL_H, phân tầng bất biến trong dải rộng, ghi số vào DAC_TA_TOOL37.md; ngưỡng không có plateau = bỏ tín hiệu đó, ghi lại lý do. B3 RED-TEAM VÒNG 1 TRÊN PROTO ĐÃ VÁ (bản vá cũng là code mới — tiền lệ vòng vá tự đẻ 2 lỗi CAO): chạy lại nguyên bộ attack_syn S1-S17 + scan_* + v_* của 2 red-team (kỳ vọng: S1/S2/S3/S4/S5/S16a/S16b/S17 đều bị bắt hoặc lộ vết) + agent tấn công góc mới. B4 TÍCH HỢP tools_core.py: port module, đăng ký tool #37, nối cổng MAX_BAN_VE + câu quá-cỡ #36 + garble-normalizer 2 phía cho nhan_chua; diff hàm #36 = 0 byte; SYSTEM_PROMPT hash 239e8b7b không đổi (nếu buộc đổi → DỪNG hỏi user). B5 VIẾT tests/test_bang_ke_khung.py (danh sách ca_test_moi), chạy qua harness/scripts/check.sh — KHÔNG pytest. B6 CỔNG TỔNG: check.sh xanh toàn bộ; K1-K5 chạy trên kết quả tool THẬT qua mcp_bridge (không chỉ proto); do_ro_neo.py + v_trunglap.py đo lại rổ/mốc so trần khai. B7 RED-TEAM VÒNG 2 SAU TÍCH HỢP (2 tầng, bắt buộc theo memory): rr_lib guard round trên 5 file đích + ≥5 file kích-hoạt-mới (kien truc 9T, 3T12P, GiaLoc KC, THPT, PCCC), thêm bộ câu bịa của Góc 1 (gồm ca '1,94 m' trên C1); agent tự-repro engine thật. Chỉ sau B7 sạch phát-hiện-CAO mới đề nghị commit/LIVE — không mark done trước validate độc lập. NGOÀI LÁT (không tự khởi động): van nhường #36 · grounding có-đơn-vị ×1000 (mcp_bridge) · đọc hàng chữ · bảng trong INSERT block · lỗ số-ĐẾM.

## Ca test mới

[
 "G-01 FLAGSHIP-A2: #37(A2, nhan_chua='đáy kênh' CHUẨN) → hàng 'đáy kênh hoàn trả' có min=1.740; nhan_chua='đáy cống' → min=1.840. ĐỐI CHỨNG chống-tautology: gọi mặc định gh60 KHÔNG chứa 1.740 (chứng minh ca đo đúng đường lọc, không phải số có sẵn trong payload mặc định — đã tự đo hôm nay). TẮT garble-normalizer → ĐỎ: lọc chuẩn trượt nhãn TCVN3 '®¸y kªnh', 0 hàng trả (tái hiện K6).",
 "G-02 QUANTIZE-01TD: #37(01-TD tuyen ong ap luc) → tồn tại hàng đọc/từ-chối-đích-danh có nhãn chứa 'PHñ' (hoặc dạng chuẩn hoá) và giá trị '+2.93'. ĐỐI CHỨNG: in ys hai khung (đuôi .194 vs .196, lệch 0.002 — số đo độc lập bằng ezdxf thô, không qua khung_doc). TẮT V-D → ĐỎ: 0 hàng, dải 20 giá trị rơi im (tái hiện v_c_probe2 hôm nay).",
 "G-03 FAIL-CLOSED-C1: đếm độc lập bằng ezdxf thô số text-số trong bbox các bảng-sống của C1, so với (ô trong hàng đọc + hàng từ chối + so_o_ngoai_hang + so_dai_bo_qua) — phải cân bằng. ĐỐI CHỨNG: bộ đếm độc lập KHÔNG import khung_doc (chống tautology bộ-trích-tự-chấm). TẮT V-C → ĐỎ: hụt 53 ô (C1) / 28 ô (C2) như đo hôm nay.",
 "G-04 TRỘN-HÀNG-S2: fixture dxf_attack S2 (2 hàng mất vạch giữa) → hàng bị từ chối 'nghi_tron_hang_mat_vach' HOẶC nhãn 'Cao do cong don' xuất hiện trong payload (o_chu). ĐỐI CHỨNG: fixture cùng dữ liệu CÓ vạch → 2 hàng đọc bình thường (detector không giết bảng lành). TẮT V-B → ĐỎ: 1 hàng b da_chung_minh=True, nhãn thứ hai biến mất 100% (tái hiện attack_syn S2).",
 "G-05 GATE-1PT-S1: fixture 1 phương trình (12−5=7) → da_chung_minh=False, _vitri.chung_minh.so_phuong_trinh=1. ĐỐI CHỨNG: fixture 3 phương trình xen kẽ → True (gate PHÂN BIỆT được, không phải tắt claim vô điều kiện). TẮT V-A → ĐỎ: True cả hai.",
 "G-06 F5-23B: F5 → đủ 23 hàng b (0 hàng mất — cấm 9-bỏ-làm-10), đúng 9 da_chung_minh=True; kiểm lại 74/74 phương trình bằng Decimal parse từ CHUỖI payload, không tin trường bool. ĐỐI CHỨNG: đẳng thức tính độc lập trong test. TẮT V-A → ĐỎ: 23/23 True (phân bố so_pt {1:14,2:2,…} tự đo hôm nay).",
 "G-07 TRÀN-MÉP-S17: fixture s17_cat_duoi.dxf → tran_mep_khung=True + khong_day_du=True + da_chung_minh=False. ĐỐI CHỨNG: fixture cột cuối nằm TRONG khung → cờ tắt, claim giữ. TẮT V-G → ĐỎ: khai đầy đủ, cong_don_cuoi=5 trong khi hình 7.5.",
 "G-08 ANCHOR-S5/S16: fixture S5 → HANG A nho_nhat='0.5'; S16a → band '41/42' KHÔNG biến mất; S16b → dau_tien/cuoi_cung đúng thứ tự hình. ĐỐI CHỨNG: fixture không-căn-lề cho kết quả Y HỆT trước/sau vá (vá không phá đường insert). TẮT V-F → ĐỎ cả ba (tái hiện attack_syn).",
 "G-09 LỌC-TRƯỢT: A1 + nhan_chua='chuoi_khong_co_xyz' → trạng thái 'loc_khong_khop', KHÔNG phải 'khung_khong_bang'. ĐỐI CHỨNG: A1 không lọc trả 12 hàng (file thật sự có bảng). TẮT V-E → ĐỎ: trả 'máy không nhận ra hàng dữ liệu nào' — mệnh đề sai về chính kết quả máy.",
 "G-10 SAI-LOẠI-LỖI: đường dẫn không tồn tại → 'loi_mo_file' (không phán 'lỗi cấu trúc DXF'). ĐỐI CHỨNG: 1 file DXFStructureError thật trong 9 file hỏng → vẫn 'loi_doc_file' đúng loại. TẮT V-E → ĐỎ: FileNotFoundError thành 'xuất lại file DXF từ bản gốc'.",
 "G-11 PROSE-K1K2: import chuỗi prose THẬT từ code (8 nhánh + 6 ly_do), _collect_numbers = rỗng + không chứa cụm _REFUSAL_MARKERS. ĐỐI CHỨNG self-check: bơm 1 chữ số vào bản sao chuỗi → ca phải báo đỏ trên bản sao (phép đo nhìn thấy lỗi). TẮT bất kỳ prose mới nào chứa %d → ĐỎ.",
 "G-12 N2-ĐẦY-ĐỦ: rổ sau B._strip_neo ⊆ số suy từ nguyên văn ô/nhãn-ĐỌC/o_chu ĐÃ TRẢ, trên 5 file × gh∈{1,6,12,60,200} × nhan_chua∈{None,garble,chuẩn}. ĐỐI CHỨNG kiểu N3/N4 #36: 987654 bơm vào _vitri (so_dai_bo_qua/muc_luc/chung_minh) phải BỊ LOẠI, đặt ngoài _vitri phải VÀO rổ. TẮT đặt-trong-_vitri của counter mới → ĐỎ: số đếm thành bằng chứng bịa.",
 "G-13 MINMAX-BẤT-BIẾN: min/max/dau/cuoi bất biến với gh∈{1,6,12,60,200} trên cả 5 file (kể cả hàng b và hàng tràn mép — bất biến trên phần-trong-khung). ĐỐI CHỨNG: len(gia_tri) PHẢI đổi theo gh (phép đo thấy tác động ngân sách). TẮT trọn-hàng → ĐỎ kiểu R3 #36 (tiền lệ 2.930 vs 2.710).",
 "G-14 SELECTIVITY-DIFF: chạy corpus 142 file, diff với baseline jsonl đóng băng ở B0 — file đổi trạng thái/tier NGOÀI danh sách kỳ vọng (01-TD, 2.Tdoc, ≤29 hàng lưới-trục, 5 hàng dedup) = 0. ĐỐI CHỨNG: baseline là file bất biến trước vá, diff hai chiều. TẮT V-H → ĐỎ: 'D2-4 (220x300)'→['+3.550','+3.550'] vẫn là hàng a.",
 "G-15 QUÁ-CỠ: file vượt MAX_BAN_VE (mock cỡ) → #37 trả NGUYÊN VĂN câu từ chối quá-cỡ #36 (assert bằng == với chuỗi import từ code #36). ĐỐI CHỨNG: file nhỏ không kích hoạt. TẮT cổng → ĐỎ: 212MB → RSS 1.682MB giết cloud (số v_chiphi).",
 "G-16 HỒI-QUY-#36: hash tests/test_bang_trac_doc.py + hash vùng hàm #36 trong tools_core.py không đổi; 35/35 PASS qua check.sh. ĐỐI CHỨNG: đây là chốt additive — mọi sửa lỡ tay vào #36 → ĐỎ ngay ở hash trước cả khi test chạy.",
 "G-17 DEDUP-GIALOC: GiaLoc KC → handle nhãn 'GT-9820B' xuất hiện đúng 1 lần toàn payload. ĐỐI CHỨNG: trước vá 4 lần (v_ktkc.py). TẮT V-I → ĐỎ.",
 "G-18 MỤC-LỤC-CẮT: A2 gọi mặc định gh60 → 'đáy kênh hoàn trả' nằm trong muc_luc_hang_chua_tra (model biết hàng tồn tại); đồng thời các nhãn mục lục KHÔNG bơm số vào rổ neo (_collect_numbers sau _strip_neo không chứa số từ nhãn mục lục). ĐỐI CHỨNG: chính phép đo hôm nay — gh60 payload không có 1.740 nhưng hàng phải hiện diện ở mục lục. TẮT V-J → ĐỎ: hàng biến mất khỏi mọi dấu vết ở gọi mặc định."
]

## Xử lý N2/R2 trung thực

TRUNG THỰC, KHÔNG SỬA TEST CHO XANH: (1) Bước 1 additive KHÔNG chạm tests/test_bang_trac_doc.py — Góc 3 đã chạy thật 35/35 PASS tại HEAD d690e3d; nghiệm thu khoá bằng hash file test không đổi. (2) 8/20 cấu hình HEAD đang vi phạm N2/R2-định-nghĩa-hẹp là NỢ CÓ SẴN của #36 (N2 chỉ nhận 'that' từ gia_tri nên min/max ngoài phần cắt lọt ra ngoài định nghĩa) — ghi công khai vào DAC_TA_TOOL37.md + harness/session-handoff.md là nợ chuyển cho lát van, KHÔNG che, KHÔNG mở rộng định nghĩa hẹp sang cấu hình mới, KHÔNG nới lỏng để 'đỡ xấu'. (3) Test #37 mới dùng định nghĩa ĐẦY ĐỦ K3 (chuẩn CAO hơn #36 hiện hành, khai rõ điều đó): rổ sau _strip_neo ⊆ số suy từ nguyên văn gia_tri/day_*/min-max/dau-cuoi/nhãn-hàng-ĐỌC/o_chu, chạy trên 5 file đích × gh∈{1,6,12,60,200} × nhan_chua∈{None, garble, chuẩn-hoá}. (4) Lát van (bước 2, NGOÀI lát này): công bố TRƯỚC danh sách 6 ca được-phép-đổi-định-nghĩa D2/D3/D5/N1/R3(+R2 cấu hình lọc), kèm bằng chứng Góc 3 V7 (V1 đổ 4 ca, V2 đổ 2 ca, N1 đổ mọi biến thể vì tập giá-trị-còn-trả trên A1 rỗng ⇒ KHÔNG TỒN TẠI van giữ nguyên 35 ca) — đây là thay đổi hợp đồng được khai báo trước bằng số, khác bản chất với sửa-test-cho-xanh; N1 viết lại thành 'giá trị CÒN TRẢ vẫn vào rổ; nếu van chặn hết hàng thì rổ phải RỖNG'.

## TIÊU CHÍ NGHIỆM THU (đặt trước — trượt = NO_GO)

ĐẶT TRƯỚC — trượt BẤT KỲ mục nào = NO_GO (xoá __pycache__ trước mọi lần đo; mẫu đo phủ corpus, không đo mẫu con): (1) NỀN 5 FILE — python do_5file.py sau toàn bộ vá: tầng a 2.556/2.556 ô khớp handle nền, min/max 226/226; tầng b 171/171 ô; hàng nền mất=0, hàng thêm ngoài nền=0; 0/391 hàng nền bị V-H/V-B hạ tầng oan. (2) 0 HỒI QUY SỐ-ĐÚNG A1/A2: 6 hàng #36 đang trả ĐÚNG phải được #37 trả đúng y giá trị (đối chiếu v_hoiquy_kq.json); A1 'Khoảng cách giữa các mặt cắt' min=2.0; A2 'đáy cống' min=1.840 và 'đáy kênh' min=1.740 qua nhan_chua chuẩn-hoá (ca G-01). (3) F5: 23/23 hàng b trả đủ 2 dãy + 8 hàng 'Góc quay' từ chối + bảng stub #24 từ chối; đúng 9 hàng da_chung_minh=True, 74/74 phương trình kiểm độc lập bằng Decimal trong test. (4) FAIL-CLOSED: tổng dải rơi-không-vết toàn corpus 142 file = 0 (hôm nay 5.359 — đếm bằng script độc lập kiểu v_tonghop); 01-TD và 2.Tdoc cấp nước thoát im lặng (mỗi file ≥1 hàng đọc hoặc từ-chối/đếm đích danh). (5) RỔ NEO CÓ TRẦN: 5 file đích gh60 Σ mốc-mm (lưới 100..20000/100) ≤ 60 và Σ mốc-mét (0..30/0.1) ≤ 90 (khai hôm nay 55/85 + dung sai vá ≤5, MỌI số tăng phải giải trình từng mốc); corpus-wide union mốc mm ≤ 143 (133+10); K3 đóng kín (rổ ⊆ nguyên văn đã trả) 0 vi phạm trên 30 file kích hoạt × cấu hình G-12; 0 handle/tham-số/prose-số lọt (K1-K5 PASS 30/30 file, đo qua mcp_bridge THẬT). (6) SELECTIVITY: diff corpus so baseline B0 — 0 file đổi trạng thái ngoài danh sách kỳ vọng; 9/9 DXFStructureError vẫn 'loi_doc_file'; 15/15 bảng thật C1/C2 sống; ≥25/29 hàng pairing chế tạo bị hạ. (7) NGƯỠNG MỚI: từng ngưỡng (NGƯỠNG_TRỘN, NGƯỠNG_CAO, đệm mép, K ap-rác) có sweep plateau in ra như do_tol.py — ngưỡng knife-edge = bỏ tín hiệu, không ship. (8) CỔNG SẢN PHẨM: bash harness/scripts/check.sh xanh TOÀN BỘ (35/35 ca #36 + toàn bộ ca G-01..G-18); KHÔNG pytest; hash tests/test_bang_trac_doc.py + hàm #36 + SYSTEM_PROMPT 239e8b7b không đổi. (9) RED-TEAM SAU-CODE bắt buộc 2 vòng (B3 trên proto-đã-vá, B7 sau tích hợp trên 5 file đích + ≥5 file kích-hoạt-mới): 0 phát hiện CAO chưa xử lý; bộ câu bịa Góc 1 (kể cả '1,94 m' C1, 'Chieu dai doan 30000 mm' F5) chạy lại — số câu LỌT không tăng so với 7 lượt đã khai ở gh60. (10) KHÔNG OVERCLAIM: kết quả chỉ được mô tả 'đọc đúng trong hệ vẽ đã đo'; chưa mark done milestone recall-hệ-khác (chỉ 01-TD/2.Tdoc là bằng chứng mới, chưa phải validate độc lập diện rộng).

## ⏳ VIỆC CHỜ USER QUYẾT

[
 "GIÁ ×1000 KHI BẬT #37 (không tự quyết): đọc F5 + đọc đầy đủ C1/C2/A1 ở mặc định gh60 mở +3 mốc-mm/+15 mốc-mét/+7 lượt-câu-bịa trên 5 file; corpus-wide 26 file #36-đang-im mở 1-53 mốc/file (union 133 vs 52, ~2,5×); phiên nhiều lượt tích luỹ ~2×. Chọn: (a) chấp nhận giá, LIVE #37 và xếp lát 'grounding có-đơn-vị' ở mcp_bridge NGAY SAU (khuyến nghị của cả 2 red-team — đòn bẩy gốc, không bóp tool đọc); (b) hoãn LIVE #37 tới khi lát ×1000 xong. Số hai phía đã đo đủ ở trên.",
 "TRẦN NGÂN SÁCH: giữ mặc định 60/trần 200 (đề xuất) hay nâng? Hàng 'đáy kênh hoàn trả' 297-giá-trị chỉ lấy được qua nhan_chua; FULL = 644 neo/180 mốc (×3,5 bề mặt). Nâng trần đổi recall-danh-sách lấy bề mặt bịa — số hai phía trong đặc tả.",
 "V-H LƯỚI TRỤC — MỨC PHẠT: hạ 29 hàng pairing-chế-tạo (3,5% hàng corpus, số verbatim thật nhưng cặp nghĩa chưa chứng minh) xuống TỪ CHỐI đích danh (đề xuất — đúng chuẩn 'đọc đúng hoặc từ chối rõ') hay chỉ GẮN CỜ giữ đọc (giữ recall, rủi ro model bỏ qua cờ)?",
 "THỨ TỰ ƯU TIÊN NGÂN SÁCH: giữ trên-xuống/trái-phải + mục lục V-J (đề xuất, hành vi dự đoán được) hay đổi sang ưu-tiên-bảng-nhiều-hàng-a/b như Góc 2 gợi ý (lấy được nhiều hàng dữ liệu hơn ở gọi mặc định nhưng thứ tự khó dự đoán, cần đo lại toàn bộ trần rổ neo)?"
]

## Rủi ro tồn dư

[
 "Kênh ×1000 của _is_grounded (mcp_bridge:928) — NGOÀI lát, bề mặt nở theo #37; chỉ giảm được bằng lát grounding-có-đơn-vị riêng. Câu bịa mm được ô-mét bảo lãnh vẫn LỌT sau lát này.",
 "Lỗ số-ĐẾM không hàng rào ('9999 cột' lọt với rổ rỗng) — của HEAD, đồng đều #36/#37, lát riêng đã có trong memory.",
 "Nhãn hàng TỪ CHỐI + mục lục bị strip khỏi rổ neo: model trích đúng một số CÓ THẬT từ nhãn từ-chối sẽ bị guard chặn oan (đo được 2 giá trị/5 file) — đánh đổi cố ý, giữ.",
 "Recall hệ-vẽ-khác: V-D mới chứng minh trên 01-TD (và kỳ vọng 2.Tdoc); các hệ vẽ chưa gặp vẫn có thể 0 hàng — nhưng sau V-C thất bại buộc phải LỘ, không im.",
 "Bảng nằm trong INSERT block (nét không nổ ra modelspace) → vẫn khong_khung; MTEXT ô số chưa từng gặp thật; bảng xoay 90° ngoài phạm vi — cả ba ghi giả định, chưa đo tần suất.",
 "Ngưỡng mới (NGƯỠNG_TRỘN/NGƯỠNG_CAO/đệm mép) hiệu chỉnh trên corpus hiện có — rủi ro overfit quy ước bản vẽ còn lại dù có gate plateau; thất bại của chúng được thiết kế để LỘ (từ chối đích danh, không im).",
 "P/R=1.0 vs nền vẫn một phần vòng tròn — validate độc lập thật sự (bản vẽ nguồn khác có nền ô-level) chưa có; cấm dùng số này để claim tổng quát.",
 "Phiên tích luỹ rổ neo qua nhiều lượt nhan_chua tiến về FULL (~2× single-call) — bản chất recall, không chặn được mà không giết mục đích tool; ghi nhận để đọc số đo cho đúng."
]

## Số KHÔNG được trích lại (đã bác/đính chính)

[
 "'+3 mốc mm (+5,8%)' của đặc tả — under-scoped: lưới MÉT +21% (70→85), corpus-wide union 133 mốc vs 52 (~2,5×), phiên nhiều-lượt tích luỹ ~2× single-call. Số đúng phải khai CẢ 3 chiều.",
 "'A2 ca chẩn đoán cũ min=1.840' — TỰ ĐO LẠI: không có trong gọi mặc định gh60 (cả 1.840 lẫn 1.740 vắng); 1.740 vượt trần 200 ở gọi cả-bảng, chỉ lấy được qua nhan_chua garble. Trình bày như 'tool trả ra' là overclaim.",
 "'min/max/dau_tien/cuoi_cung LUÔN tính trên TRỌN hàng' — sai khi cột cuối tràn mép khung (S17: cong_don_cuoi=5 vs hình 7.5, khong_day_du=False). Sau vá V-G phải viết lại thành 'trọn phần trong khung, kèm cờ tràn mép'.",
 "'thất bại phải LỘ' / fail-closed — sai trước vá: 5.359 dải corpus rơi im, tự xác nhận 01-TD 20 giá trị + nhãn thật biến mất không vết.",
 "'378/462 ô tự-chứng-minh (81,8%)' — sau gate so_pt≥2+xen_ke còn 129 ô đẳng thức + 207 chéo = 336/462 (72,7%); số cũ chỉ đúng với định nghĩa claim lỏng.",
 "'P/R=1.0 vs nền' — một phần vòng tròn (proto tái hiện thước-1 của nền; tác giả tự khai) — không được dùng làm bằng chứng ngoài-hệ.",
 "RAM 1.682MB/72,4s file 212MB — đo 1 lần 1 máy local; dùng làm ràng buộc thiết kế (giữ cổng MAX_BAN_VE), không dùng làm cam kết hiệu năng.",
 "'0 dương tính giả đẳng thức trên 137 file' — Góc 3 ghi chú 4 file >60MB chưa kiểm chéo; coi là 0-trên-tập-đã-kiểm."
]
