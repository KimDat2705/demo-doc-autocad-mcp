# File tạm / đầu ra — KHÔNG cần lên repo
__pycache__/
*.pyc
_uploads/
_renders/
_hoc_log/
# khao_sat_corpus.py: .dxf convert ra + bao_cao.json (dan xuat, tai tao duoc)
# LUU Y: .gitignore KHONG co comment cuoi dong -> comment phai o DONG RIENG.
_khao_sat/
spikes/out_*.png
spikes/spike_bridge_out.txt
.env

# Test artifacts (kết quả chạy, log) — giữ SCRIPT test, bỏ dữ liệu chạy
tests/battery_results*.jsonl
tests/*.log
tests/verify*_chunks/
tests/battery.json
tests/kichban*_ketqua.*

# AN DANH DU LIEU MAU: duong dan corpus THAT (ten thu muc/file khach hang) — KHONG commit.
# Chi commit corpus_local.example.py (mau, khong co ten that). Xem harness/QUY_TRINH_AN_DANH_DU_LIEU_MAU.md
tests/corpus_local.py

# LƯU Ý: vendor/ODAFileConverter.deb PHẢI commit lên repo (Render build Docker cần) -> dùng repo PRIVATE.
